import { Tab } from "../../tabSection/Tab";

type SetsFieldProps = {};

export const SetsField = ({}: SetsFieldProps) => {
  return (
    <div>
      <Tab id={1} onRemove={() => {}} tabName={"Set"} />
    </div>
  );
};
